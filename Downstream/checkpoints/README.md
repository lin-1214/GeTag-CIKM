# Pre-trained model checkpoints

Place the pre-trained UniSRec checkpoint here:
- UniSRec-FHCKM-300.pth

This directory is tracked in git to include the pre-trained model.
Training outputs go to output/ directory (gitignored).
